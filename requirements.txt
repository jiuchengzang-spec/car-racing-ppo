# Core: physics, env, and human play.
numpy>=1.26
gymnasium>=1.0
# pygame-ce is a drop-in fork of pygame (still `import pygame`) that ships
# prebuilt wheels for new Python versions much sooner — upstream pygame has no
# wheel for Python 3.14 yet and tries (and fails) to compile from source.
pygame-ce>=2.5

# RL training / playback (optional — only needed for train.py and enjoy.py).
# Pure-PyTorch PPO lives in train.py — no stable-baselines3, no TensorBoard.
# The default Linux wheel is CUDA-enabled and train.py auto-uses the GPU. Pick a
# build matching your machine if the default doesn't fit:
#   GPU (CUDA 13):  pip install torch --index-url https://download.pytorch.org/whl/cu130
#   CPU-only:       pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Optional: Weights & Biases experiment tracking — only needed for `train.py --wandb`
# (lazy-imported, so console/CSV logging works without it). Run `wandb login` once.
wandb>=0.16
